{
  "name": "simple-app",
  "private": true,
  "workspaces": [
    "auth",
    "clubs",
    "events",
    "gateway",
    "notifications",
    "users"
  ],
  "scripts": {
    "dev": "concurrently --raw -n auth,clubs,events,gateway,notifications,users \"npm run dev --workspace auth\" \"npm run dev --workspace clubs\" \"npm run dev --workspace events\" \"npm run dev --workspace gateway\" \"npm run dev --workspace notifications\" \"npm run dev --workspace users\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "axios": "^1.13.2"
  },
  "scripts": {
    "compare:requests": "node tools/compare-scenarios.js"
  }
}



